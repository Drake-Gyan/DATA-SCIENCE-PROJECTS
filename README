download the datasets that will be used in all this three models. Unzip it and put the content under main folde.

install required python packages if previously not installed

finally run on Jupyter Notebook
